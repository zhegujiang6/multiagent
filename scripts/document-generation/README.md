# 文档生成工具

安装依赖：

```powershell
python -m pip install -r scripts/document-generation/requirements.txt
```

从项目根目录运行任一脚本，例如：

```powershell
python scripts/document-generation/generate_analysis_doc.py
```

生成的 Word 文档统一写入 `docs/design/`。
